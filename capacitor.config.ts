import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'aob.uniform.cafi.com',
  appName: 'AoB Uniform',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
